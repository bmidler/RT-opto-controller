# Controller dependencies. Everything the controller imports is either listed
# here or vendored under utils/ -- there are no sibling-repo dependencies.
torch
numpy
opencv-python
imageio-ffmpeg
pyyaml
pyserial          # only needed when driving real Arduinos (serial_dry_run: false)

# Camera SDK (install per FLIR's instructions; not on PyPI):
#   PySpin (Spinnaker SDK)  -- required only for source: "flir"
